/*
let textLine = "this is some text line with JavaScript code";
function vowel(textLine)

{
  
    const matches = textLine.match(/["aeiouAEIOU"]/gi);
        
    return matches ? matches.length : 0;
}

console.log(vowel(textLine));





/*
for(let i = 0; i <= textLine.length; i++)
{
 if 
  (textLine[i]=== 'a' || textLine[i] === 'e' || textLine[i] === 'i' || textLine[i] === 'o' || textLine[i] === 'u')
   
    console.log(textLine[i]);
}
*/
/*
let nameList =["HTML", "JAVA", "JavaScript","python"];
for(let i in nameList){
  console.log(nameList[i]);
  

}

let n=0;
while(n<5){
  console.log("count of N:" + n);
  ++n;
  
}


let v=5;
do{
  console.log(v);
  v++;
  
}
while (v<=15);
*/
/*
function repeatStringNumTimes(string, times) {
  let repeatedString = "";
  while (times > 0) {
    repeatedString += string;
    times--;
  }
  return repeatedString;
}
console.log
 (repeatStringNumTimes("HI", 15));
*/

